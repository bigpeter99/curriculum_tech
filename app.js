function toggleMode() {
  const html = document.documentElement
  html.classList.toggle("light")

  // vamos agora pegar e colocar uma imagem para poder servir de bonus para animacao do site
  // pegar a tag image e substituir a imagem

  // vamos primeiro pegar a imagem
  /*const img = document.querySelector("#profile img")

  if (html.classList.contains("light")) {
    img.setAttribute("src", "./assets/avatar-001.png")
  } else {
    img.setAttribute("scr", "./assets/avatar.png")
  }*/
}